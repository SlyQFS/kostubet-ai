version: "3.8"

services:
  kostubetai:
    build: .
    container_name: kostubetai
    restart: unless-stopped
    init: true
    env_file: .env
    volumes:
      - kostubetai-data:/app/data

volumes:
  kostubetai-data:
